/* Angular Flash Cards CSS */
.flashcard-icon {
  font-weight: bold;
}

.flashcard-icon:hover {
  cursor: pointer;
}

.cards-show {
  position: fixed;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  z-index: 10;
  background-color: black;
  opacity: 0.8;
}

.cards-hide {
  display: none;
}

.card {
  position: fixed;
  padding: 10px;
  top: 10%;
  left: 50%;
  z-index: 20;
  width: 560px;
  margin-left: -280px;
  background-color: white;
  border: 1px solid #999;
  border-radius: 5px;
  outline: none;

  /*&.fade {
    .transition(e('opacity .3s linear, top .3s ease-out'));
    top: -25%;
  }
  &.fade.in { top: 10%; }*/
}

.card-content {
  min-height: 200px;
}

.card-button {
  float:right;
  margin-right: 5px;
  font-size: 14px;
  font-weight: bold;
  cursor: pointer;
}

.close-button {
  float:right;
  margin-right: 5px;
  font-size: 14px;
  font-weight: bold;
  cursor: pointer;
}
