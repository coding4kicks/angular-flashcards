/* Angular Flash Cards */
'use strict';
angular.module('flashcard', []);

angular.module('flashcard').directive('flashcards', function() {
  return {
    replace: false,
    template: '<div><h1>here</h1></div>',
  };
});
